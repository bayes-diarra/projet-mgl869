
import 'package:flutter/material.dart';

class AcceptDelivery extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
        title: Text('Accept Delivery'),
        ),
        body: Center(
          child: Text("AcceptDeliveryForm")//AcceptDeliveryForm(),
        )
    );

  }
}