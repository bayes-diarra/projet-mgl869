
import 'package:flutter/material.dart';

class AvailableDeliveries extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Available Deliveries'),
        ),
        body: Center(
          child: Text("AvailableDeliveryForm")//AvailableDeliveryForm(),
        )
    );
  }
}