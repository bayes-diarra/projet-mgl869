import 'package:flutter/material.dart';
class AddProduct extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Accept Delivery'),
        ),
        body: Center(
          child: Text("AddProductForm")//AddProductForm(),
        )
    );
  }
}